# dashboard screenshots

once the .pbix is built in Power BI Desktop and the report pages exist, export each page as PNG (File -> Export -> Export to PDF then crop, or just screenshot at 1920x1080) and drop them here named:

- `01-overview.png`
- `02-geography.png`
- `03-customers.png`

then reference them from the root `README.md`.
